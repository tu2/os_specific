~ OpenBSD~
~ basic settings ~

## (if the user is not yet added to wheel)
## add user to wheel:

usermod -G wheel ~username~

## to enable ~doas~, add this to /etc/doas.conf:

permit setenv { PKG_PATH ENV PS1 SSH_AUTH_SOCK } :wheel

## to add a repository (from the internet):

export PKG_PATH=http://www.mirrorservice.org/pub/OpenBSD/5.8/packages/amd64/

## export path to ~/.profile:

export PKG_PATH=http://www.mirrorservice.org/pub/OpenBSD/$(uname -r)/packages/$(machine -a)

## in 6.1 there is an option for an installurl file (/etc/installurl):

su -l
echo "https://www.mirrorservice.org/pub/OpenBSD" >> /etc/installurl

## to install programs:

## become 'root' using 'su' and install programs:

pkg_add ~program~

## or install programs using 'doas':

doas pkg_add ~program~

## SECURITY

## OpenBSD syspatch

su
syspatch

## M:Tier

curl -O https://stable.mtier.org/openup
chmod +x openup
./openup

pkg_add -u

##END
